server {
  listen 80;
  server_name ${APP_DOMAIN} ${MEDIA_DOMAIN};

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 200 "TLS certificate bootstrap in progress\n";
    add_header Content-Type text/plain;
  }
}
